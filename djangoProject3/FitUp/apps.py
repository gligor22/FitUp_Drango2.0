from django.apps import AppConfig


class FitupConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'FitUp'
    # from scheduler import scheduler
    # scheduler.start()
